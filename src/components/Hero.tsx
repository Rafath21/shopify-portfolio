import React from 'react';

const Hero = () => {
  return (
    <section id="hero" className="w-full min-h-screen flex items-center justify-center bg-gray-100">
      {/* Content from page.tsx moved here */}
      <h1 className="text-4xl font-bold">Hero Section</h1>
      {/* TODO: Add actual hero content like name, title, brief intro */}
    </section>
  );
};

export default Hero;
